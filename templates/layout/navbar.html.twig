<!-- Responsive navbar-->
    <nav class="flex bg-neutral-200 justify-between justify-items-center py-3 px-10">
            <h1 class="font-serif italic text-2xl p-4 ml-4">ACME</h1>
        <div class="flex  justify-between px-8">
            <a class="p-4 text-center" href="{{path('accueil')}}">Accueil</a>
            <a class="p-4" href="{{path('aPropos')}}">A propos</a>
            {% if app.session.get('role') is defined and app.session.get('role') is not null %}
                {% if app.session.get('role') == 'Admin' %}
                    <a class="p-4" href="{{ path('app_admin_create_product') }}">CRUD</a>
                {% endif %}
            <a class="p-4" href="{{ path('app_menu') }}">Menu</a>
            <a class="p-4" href="{{ path('app_panier') }}">Panier</a>
            <a class="p-4" href="{{ path('logout') }}">Se déconnecter</a>
            {% else %}
            <a class="p-4" href="{{ path('login') }}">Se connecter</a>
            {% endif %}
        </div>
    </nav>